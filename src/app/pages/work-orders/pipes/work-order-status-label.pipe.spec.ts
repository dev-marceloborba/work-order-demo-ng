import { WorkOrderStatusLabelPipe } from './work-order-status-label.pipe';

describe('WorkOrderStatusLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new WorkOrderStatusLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
